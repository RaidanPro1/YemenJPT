<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>Root Admin | RaidanPro Mesh</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Tajawal', sans-serif; background-color: #020617; color: #f8fafc; }
        .glass { background: rgba(30, 41, 59, 0.7); backdrop-filter: blur(12px); border: 1px solid rgba(255,255,255,0.05); }
    </style>
</head>
<body class="flex min-h-screen">
    <!-- Sidebar -->
    <aside class="w-72 glass border-l border-slate-800 p-8 flex flex-col">
        <div class="mb-12 flex items-center gap-4">
            <div class="w-10 h-10 bg-teal-600 rounded-xl flex items-center justify-center font-black">R</div>
            <span class="text-xl font-black tracking-tight">Raidan<span class="text-teal-500">Pro</span></span>
        </div>
        <nav class="flex-1 space-y-2">
            <a href="/admin" class="flex items-center gap-4 px-6 py-4 rounded-2xl <%= activePage === 'overview' ? 'bg-teal-600/20 text-teal-400 border border-teal-500/30' : 'text-slate-400 hover:bg-white/5' %>">
                <span>الرئيسية</span>
            </a>
            <a href="/admin/branding" class="flex items-center gap-4 px-6 py-4 rounded-2xl <%= activePage === 'branding' ? 'bg-teal-600/20 text-teal-400 border border-teal-500/30' : 'text-slate-400 hover:bg-white/5' %>">
                <span>تخصيص الهوية</span>
            </a>
            <a href="/admin/tenants" class="flex items-center gap-4 px-6 py-4 rounded-2xl <%= activePage === 'tenants' ? 'bg-teal-600/20 text-teal-400 border border-teal-500/30' : 'text-slate-400 hover:bg-white/5' %>">
                <span>إدارة المستأجرين</span>
            </a>
            <a href="/admin/ai" class="flex items-center gap-4 px-6 py-4 rounded-2xl <%= activePage === 'ai' ? 'bg-teal-600/20 text-teal-400 border border-teal-500/30' : 'text-slate-400 hover:bg-white/5' %>">
                <span>AI Nexus</span>
            </a>
            <a href="/admin/security" class="flex items-center gap-4 px-6 py-4 rounded-2xl <%= activePage === 'security' ? 'bg-teal-600/20 text-teal-400 border border-teal-500/30' : 'text-slate-400 hover:bg-white/5' %>">
                <span>الأمن والولوج</span>
            </a>
        </nav>
        <div class="pt-8 border-t border-slate-800">
            <p class="text-[10px] text-slate-500 uppercase font-black">Root Commander</p>
            <p class="text-xs font-bold text-slate-300">Zaid Al-Yamani</p>
        </div>
    </aside>

    <!-- Main Content -->
    <main class="flex-1 p-12 overflow-y-auto">
        <header class="flex justify-between items-center mb-12">
            <h1 class="text-3xl font-black uppercase">إدارة النظام السيادي</h1>
            <div class="flex items-center gap-4 bg-slate-900 px-4 py-2 rounded-full border border-slate-800">
                <span class="w-2 h-2 bg-emerald-500 rounded-full animate-pulse"></span>
                <span class="text-xs font-bold uppercase tracking-widest">Mesh Operational</span>
            </div>
        </header>

        <%- body %>
    </main>
</body>
</html>