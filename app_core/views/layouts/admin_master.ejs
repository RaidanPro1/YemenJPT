
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Root Command | RaidanPro Mesh</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { font-family: 'Tajawal', sans-serif; background-color: #f8fafc; }
        .glass-sidebar { background: rgba(15, 23, 42, 0.98); backdrop-filter: blur(20px); }
        .nav-link-active { background: rgba(13, 148, 136, 0.15); color: #2dd4bf; border-right: 4px solid #0d9488; }
        .custom-scrollbar::-webkit-scrollbar { width: 4px; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: #334155; border-radius: 10px; }
        [x-cloak] { display: none !important; }
    </style>
</head>
<body class="flex min-h-screen overflow-hidden" x-data="{ openTools: false, openSettings: false, sidebarCollapsed: false, searchQuery: '' }">

    <!-- SIDEBAR -->
    <aside 
        class="glass-sidebar text-slate-300 flex flex-col h-screen border-l border-slate-800 z-50 transition-all duration-300"
        :class="sidebarCollapsed ? 'w-24' : 'w-80'"
    >
        <!-- Logo -->
        <div class="p-8 flex items-center gap-4 border-b border-slate-800/50">
            <div class="w-10 h-10 bg-teal-600 rounded-xl flex items-center justify-center text-white font-black text-xl shadow-lg shadow-teal-900/20 shrink-0">R</div>
            <div x-show="!sidebarCollapsed" x-transition>
                <h1 class="text-white font-black tracking-tight text-lg leading-none">RAIDAN<span class="text-teal-500">PRO</span></h1>
                <p class="text-[10px] uppercase tracking-widest text-slate-500 mt-1 font-bold">Root Architecture</p>
            </div>
        </div>

        <!-- Smart Search -->
        <div class="px-6 py-4" x-show="!sidebarCollapsed" x-transition>
            <div class="relative group">
                <i class="fa-solid fa-magnifying-glass absolute right-4 top-1/2 -translate-y-1/2 text-slate-600 group-focus-within:text-teal-500 transition-colors"></i>
                <input 
                    type="text" 
                    placeholder="بحث سريع في العقد..." 
                    class="w-full bg-slate-900/60 border border-slate-800 rounded-xl py-2.5 pr-12 pl-4 text-xs text-white focus:outline-none focus:border-teal-500 transition-all"
                    x-model="searchQuery"
                >
            </div>
        </div>

        <!-- Quick Shortcuts (Pinned Actions) -->
        <div 
            class="px-6 py-4 flex border-b border-slate-800/20 mb-2"
            :class="sidebarCollapsed ? 'flex-col items-center gap-6' : 'justify-between'"
        >
            <a href="/admin/tenants" title="إضافة مؤسسة" class="w-10 h-10 rounded-xl bg-slate-900 border border-slate-800 flex items-center justify-center text-blue-400 hover:bg-teal-600 hover:text-white transition-all shadow-inner group relative">
                <i class="fa-solid fa-plus text-sm"></i>
                <span x-show="sidebarCollapsed" class="absolute right-full mr-4 px-3 py-1.5 bg-slate-900 border border-slate-800 rounded-lg text-[10px] font-black text-white whitespace-nowrap opacity-0 group-hover:opacity-100 pointer-events-none transition-opacity z-50">إضافة مؤسسة</span>
            </a>
            <a href="/admin/security" title="Panic Mode" class="w-10 h-10 rounded-xl bg-slate-900 border border-slate-800 flex items-center justify-center text-red-500 hover:bg-red-600 hover:text-white transition-all shadow-inner group relative">
                <i class="fa-solid fa-shield-halved text-sm"></i>
                <span x-show="sidebarCollapsed" class="absolute right-full mr-4 px-3 py-1.5 bg-slate-900 border border-slate-800 rounded-lg text-[10px] font-black text-white whitespace-nowrap opacity-0 group-hover:opacity-100 pointer-events-none transition-opacity z-50">Panic Mode</span>
            </a>
            <a href="/admin/ai-nexus" title="AI Training" class="w-10 h-10 rounded-xl bg-slate-900 border border-slate-800 flex items-center justify-center text-[#e1b000] hover:bg-[#e1b000] hover:text-[#00338d] transition-all shadow-inner group relative">
                <i class="fa-solid fa-brain text-sm"></i>
                <span x-show="sidebarCollapsed" class="absolute right-full mr-4 px-3 py-1.5 bg-slate-900 border border-slate-800 rounded-lg text-[10px] font-black text-white whitespace-nowrap opacity-0 group-hover:opacity-100 pointer-events-none transition-opacity z-50">AI Nexus</span>
            </a>
            <a href="/admin/dashboard" title="تحليلات" class="w-10 h-10 rounded-xl bg-slate-900 border border-slate-800 flex items-center justify-center text-emerald-400 hover:bg-teal-600 hover:text-white transition-all shadow-inner group relative">
                <i class="fa-solid fa-chart-pie text-sm"></i>
                <span x-show="sidebarCollapsed" class="absolute right-full mr-4 px-3 py-1.5 bg-slate-900 border border-slate-800 rounded-lg text-[10px] font-black text-white whitespace-nowrap opacity-0 group-hover:opacity-100 pointer-events-none transition-opacity z-50">تحليلات</span>
            </a>
        </div>

        <!-- Navigation -->
        <nav class="flex-1 overflow-y-auto custom-scrollbar p-4 space-y-2">
            <div x-show="!sidebarCollapsed" class="px-4 text-[10px] font-black text-slate-600 uppercase tracking-widest mb-4 flex items-center justify-between">
                Core System
                <i class="fa-solid fa-layer-group opacity-20"></i>
            </div>
            
            <a href="/admin/dashboard" class="flex items-center gap-4 px-4 py-3 rounded-xl transition-all hover:bg-white/5 hover:text-white <%= activePage === 'dashboard' ? 'nav-link-active' : '' %>">
                <i class="fa-solid fa-chart-line w-5 shrink-0 text-center"></i>
                <span x-show="!sidebarCollapsed" class="font-bold text-sm">غرفة القيادة</span>
            </a>

            <!-- Tool Gateways Dropdown -->
            <div x-data="{ open: <%= ['osint', 'verification', 'monitoring', 'security_tools'].includes(activePage) %> }">
                <button @click="open = !open" class="w-full flex items-center justify-between px-4 py-3 rounded-xl transition-all hover:bg-white/5 hover:text-white">
                    <div class="flex items-center gap-4">
                        <i class="fa-solid fa-shield-heart w-5 shrink-0 text-center text-teal-500"></i>
                        <span x-show="!sidebarCollapsed" class="font-bold text-sm">بوابات الأدوات</span>
                    </div>
                    <i x-show="!sidebarCollapsed" class="fa-solid fa-chevron-down text-[10px] transition-transform" :class="open ? 'rotate-180' : ''"></i>
                </button>
                <div x-show="open && !sidebarCollapsed" x-collapse class="pr-6 mt-1 space-y-1">
                    <a href="/admin/tools/verification" class="flex items-center justify-between px-4 py-2 text-xs font-medium rounded-lg hover:text-white transition-all group <%= activePage === 'verification' ? 'text-teal-400' : 'text-slate-500' %>">
                        <span><i class="fa-solid fa-shield-halved text-[10px] mr-2"></i> وحدة التحقق</span>
                        <i class="fa-solid fa-chevron-left text-[8px] opacity-0 group-hover:opacity-100 transition-all"></i>
                    </a>
                    <a href="/admin/tools/osint" class="flex items-center justify-between px-4 py-2 text-xs font-medium rounded-lg hover:text-white transition-all group <%= activePage === 'osint' ? 'text-teal-400' : 'text-slate-500' %>">
                        <span><i class="fa-solid fa-magnifying-glass text-[10px] mr-2"></i> نظام كشاف</span>
                        <i class="fa-solid fa-chevron-left text-[8px] opacity-0 group-hover:opacity-100 transition-all"></i>
                    </a>
                    <a href="/admin/tools/monitoring" class="flex items-center justify-between px-4 py-2 text-xs font-medium rounded-lg hover:text-white transition-all group <%= activePage === 'monitoring' ? 'text-teal-400' : 'text-slate-500' %>">
                        <span><i class="fa-solid fa-broadcast-tower text-[10px] mr-2"></i> الرصد الحي</span>
                        <i class="fa-solid fa-chevron-left text-[8px] opacity-0 group-hover:opacity-100 transition-all"></i>
                    </a>
                </div>
            </div>

            <div x-show="!sidebarCollapsed" class="px-4 text-[10px] font-black text-slate-600 uppercase tracking-widest mt-8 mb-4 flex items-center justify-between">
                Management
                <i class="fa-solid fa-sliders opacity-20"></i>
            </div>

            <a href="/admin/tenants" class="flex items-center gap-4 px-4 py-3 rounded-xl transition-all hover:bg-white/5 hover:text-white <%= activePage === 'tenants' ? 'nav-link-active' : '' %>">
                <i class="fa-solid fa-building-shield w-5 shrink-0 text-center"></i>
                <span x-show="!sidebarCollapsed" class="font-bold text-sm">إدارة المؤسسات</span>
            </a>

            <a href="/admin/cms" class="flex items-center gap-4 px-4 py-3 rounded-xl transition-all hover:bg-white/5 hover:text-white <%= activePage === 'cms' ? 'nav-link-active' : '' %>">
                <i class="fa-solid fa-palette w-5 shrink-0 text-center text-[#e1b000]"></i>
                <span x-show="!sidebarCollapsed" class="font-bold text-sm">الهوية والبصمة (CMS)</span>
            </a>

            <a href="/admin/seo" class="flex items-center gap-4 px-4 py-3 rounded-xl transition-all hover:bg-white/5 hover:text-white <%= activePage === 'seo' ? 'nav-link-active' : '' %>">
                <i class="fa-solid fa-globe w-5 shrink-0 text-center"></i>
                <span x-show="!sidebarCollapsed" class="font-bold text-sm">محركات البحث</span>
            </a>

            <a href="/admin/ai-nexus" class="flex items-center gap-4 px-4 py-3 rounded-xl transition-all hover:bg-white/5 hover:text-white <%= activePage === 'ai-nexus' ? 'nav-link-active' : '' %>">
                <i class="fa-solid fa-brain w-5 shrink-0 text-center"></i>
                <span x-show="!sidebarCollapsed" class="font-bold text-sm">مركز تعلم الآلة</span>
            </a>
        </nav>

        <!-- Footer / User -->
        <div class="p-6 border-t border-slate-800/50 bg-slate-900/40">
            <div class="flex items-center gap-4" :class="sidebarCollapsed ? 'justify-center' : ''">
                <img src="https://ui-avatars.com/api/?name=Root+Admin&background=0d9488&color=fff" class="w-10 h-10 rounded-xl border border-slate-700 shrink-0">
                <div class="flex-1 min-w-0" x-show="!sidebarCollapsed" x-transition>
                    <p class="text-white font-bold text-xs truncate">Zaid Al-Yamani</p>
                    <p class="text-[10px] text-slate-500 font-bold uppercase tracking-tighter">Root Commander</p>
                </div>
                <a href="/logout" class="text-slate-500 hover:text-red-500 transition-colors shrink-0" :class="sidebarCollapsed ? 'hidden' : ''">
                    <i class="fa-solid fa-power-off"></i>
                </a>
            </div>
        </div>
    </aside>

    <!-- MAIN CONTENT -->
    <main class="flex-1 h-screen overflow-hidden flex flex-col relative">
        <!-- Topbar -->
        <header class="h-20 bg-white border-b border-slate-200 flex items-center justify-between px-10 shrink-0 shadow-sm z-40">
            <div class="flex items-center gap-6">
                <button 
                    @click="sidebarCollapsed = !sidebarCollapsed" 
                    class="p-2 bg-slate-100 rounded-lg text-slate-500 hover:bg-slate-200 transition-all shadow-inner"
                >
                    <i class="fa-solid" :class="sidebarCollapsed ? 'fa-indent' : 'fa-outdent'"></i>
                </button>
                <div class="flex items-center gap-4">
                    <h2 class="text-xl font-black text-slate-800"><%= pageTitle %></h2>
                    <div class="h-6 w-px bg-slate-200"></div>
                    <p class="text-xs font-bold text-slate-400"><%= breadcrumb %></p>
                </div>
            </div>
            
            <div class="flex items-center gap-6">
                <div class="hidden lg:flex items-center gap-2 px-4 py-2 bg-emerald-50 rounded-full border border-emerald-100 shadow-sm">
                    <span class="w-2 h-2 bg-emerald-500 rounded-full animate-pulse shadow-[0_0_8px_rgba(16,185,129,0.4)]"></span>
                    <span class="text-[10px] font-black text-emerald-600 uppercase tracking-widest">Sovereign Grid Online</span>
                </div>
                <div class="flex items-center gap-2 bg-slate-100 p-1.5 rounded-xl border border-slate-200">
                    <button class="w-8 h-8 rounded-lg flex items-center justify-center text-slate-500 hover:bg-white hover:shadow-sm transition-all">
                        <i class="fa-solid fa-magnifying-glass text-xs"></i>
                    </button>
                    <button class="w-8 h-8 rounded-lg flex items-center justify-center text-slate-500 hover:bg-white hover:shadow-sm transition-all relative">
                        <i class="fa-solid fa-bell text-xs"></i>
                        <span class="absolute top-1 right-1 w-2 h-2 bg-red-500 rounded-full border-2 border-white"></span>
                    </button>
                </div>
            </div>
        </header>

        <!-- Content Area -->
        <section class="flex-1 overflow-y-auto p-10 bg-slate-50/50 custom-scrollbar">
            <%- body %>
        </section>

        <!-- Global Search / Quick Navigation Modal (Ctrl+K) -->
        <div 
            x-show="openTools" 
            class="fixed inset-0 bg-black/80 backdrop-blur-md z-[100] flex items-center justify-center p-6" 
            x-transition
            @keydown.window.escape="openTools = false"
            @keydown.window.ctrl.k.prevent="openTools = !openTools"
            x-cloak
        >
            <div class="bg-slate-900 w-full max-w-2xl rounded-[3rem] border border-slate-800 shadow-3xl p-10 overflow-hidden animate-in zoom-in-95">
                <div class="flex items-center gap-4 mb-8">
                    <i class="fa-solid fa-terminal text-teal-500 text-xl"></i>
                    <input type="text" placeholder="أدخل أمراً أو اسم بوابة..." class="bg-transparent border-none outline-none text-white text-2xl font-black flex-1 rtl" autofocus>
                    <span class="text-slate-600 text-[10px] border border-slate-800 px-2 py-1 rounded">ESC</span>
                </div>
                <div class="grid grid-cols-2 gap-4">
                    <div class="p-6 bg-slate-800/40 rounded-2xl border border-slate-700 hover:border-teal-500 transition-all cursor-pointer group">
                        <i class="fa-solid fa-building-circle-check text-teal-500 mb-3 block text-xl"></i>
                        <h4 class="text-white font-bold">إضافة مستأجر</h4>
                        <p class="text-slate-500 text-[10px] mt-1">فتح معالج التهيئة الفوري</p>
                    </div>
                    <div class="p-6 bg-slate-800/40 rounded-2xl border border-slate-700 hover:border-red-500 transition-all cursor-pointer group">
                        <i class="fa-solid fa-radiation text-red-500 mb-3 block text-xl"></i>
                        <h4 class="text-white font-bold">Panic Button</h4>
                        <p class="text-slate-500 text-[10px] mt-1">تفعيل وضع التمويه السيادي</p>
                    </div>
                </div>
            </div>
        </div>
    </main>

</body>
</html>
